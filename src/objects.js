/**
 * Created by Yun on 2016/1/2.
 */

const GEN_ID = 'objects.genId';

const SAVE = 'objects.save';

const LOAD = 'objects.load';

const DELETE = 'objects.delete';

const UPDATE = 'objects.update';

export const OBJECTS = {
  GEN_ID,
  SAVE,
  LOAD,
  DELETE,
  UPDATE,
};
